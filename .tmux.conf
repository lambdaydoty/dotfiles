set -g prefix C-a
bind C-a send-prefix
unbind C-a

set-window-option -g xterm-keys on
set-window-option -g mode-keys vi

clipboard_port=4000

## v2.4+
#bind-key -T copy-mode-vi v send-keys -X begin-selection
#bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "nc -N localhost $clipboard_port"
#bind-key -T copy-mode-vi r send-keys -X rectangle-toggle
#
## v1.8+
#bind-key Escape copy-mode
#bind-key -t vi-copy Escape cancel
#bind-key p paste-buffer
#bind-key -t vi-copy v begin-selection
#bind-key -t vi-copy V select-line
#bind-key -t vi-copy r rectangle-toggle
#bind -t vi-copy y copy-pipe "nc -N localhost $clipboard_port"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'

set -g status-right '#(TZ="Asia/Taipei" date +%%H:%%M) Continuum:#{continuum_status}'
set -g status-left '#[bg=white] #S'
set -g @continuum-restore 'on'
set -g @continuum-save-interval '60'
set -g @override_copy_command "nc -N localhost $clipboard_port"

# ** Initialize TMUX plugin manager **
run '~/.tmux/plugins/tpm/tpm'
# ** keep this line at the very bottom of tmux.conf **
